" Enable auto indentation
set autoindent

" Enable bold text
set t_Co=256        " Enable 256 colors (for bold text)
set termguicolors   " Enable true color support

" Enable mouse support
set mouse=a

" Set tab size to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab        " Use spaces instead of tabs

" Set color for keys (for example, setting color in vim)
highlight Normal ctermfg=blue ctermbg=red

